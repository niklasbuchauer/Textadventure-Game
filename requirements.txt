# Core dependencies for Unitopia text-based MMO

# Timezone data for zoneinfo (required on Windows for timezone awareness)
# zoneinfo itself is built-in to Python 3.9+
tzdata

# Pygame UI framework
pygame-ce
pygame_gui

# Web API backend (initial browser-playable implementation)
fastapi
uvicorn[standard]
